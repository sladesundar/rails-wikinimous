<h1>Create new Article</h1>

<%= form_for @article do |article| %>
  <%= article.label :title %>
  <%= article.text_field :title %>
  <%= article.label :content %>
  <div class="ace-editor">
  <%= article.text_area :content, rows: 15, columns: 8 %>
  </div>
  <%= article.submit %>
<% end %>
